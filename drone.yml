kind: pipeline
type: docker
name: linux-amd64

platform:
  arch: amd64
  os: linux

steps:
  -  name: test
     image: alpine/git
     image: java:8-jdk
     pull: true
     
     commands:
       - chmod 777 ./Run.command
       - ./Run.command -hi
when:
    event:
      - push
      - tag
